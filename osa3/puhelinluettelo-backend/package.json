{
  "name": "puhelinluettelo-backend",
  "version": "1.0.0",
  "description": "",
  "license": "MIT",
  "author": "Elias Ventovuori",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rmdir /s /q dist && cd ..\\..\\osa2\\puhelinluettelo && npm run build && xcopy dist ..\\..\\osa3\\puhelinluettelo-backend\\dist /E /I /Y",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  }
}
